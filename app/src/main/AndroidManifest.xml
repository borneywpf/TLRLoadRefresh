<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.think.uiloader">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="com.think.uiloader.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.ListViewActivity" android:label="@string/label_listview"/>
        <activity android:name=".ui.RecyclerViewActivity" android:label="@string/label_recyclerview"/>
        <activity android:name=".ui.ViewGroupActivity" android:label="@string/label_viewgroup"/>
        <activity android:name=".ui.TextViewActivity" android:label="@string/label_textview"/>
        <activity android:name=".ui.KeepHeadActivity" android:label="@string/label_keephead"/>
        <activity android:name=".ui.CannotMoveHeadByTLRActivity" android:label="@string/label_cannot_move_head_by_tlr"/>
    </application>

</manifest>